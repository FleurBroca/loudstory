<% if user_signed_in? %>
    <ul class="nav navbar-nav navbar-gen navbar-left list-unstyled list-inline">
      <li class="active"><%= link_to 'Learn', posts_path %><span class="sr-only"></span></li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Create<span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
          <% alltracks.each do |track| %>
            <% if track.position == 1 %>
              <li><%= link_to track.title, track %></li>
              <li class="divider"></li>
            <% else %>
              <li><%= link_to track.title, votes_tracks_path %></li>
              <li class="divider"></li>
            <% end %>
          <% end %>
        </ul>
      </li>

    </ul>

    <ul class="nav navbar-nav navbar-perso navbar-right">
      <li>
        <%= link_to "Upgrade", pricing_path %>
      </li>
      <li>
        &nbsp;&nbsp;&nbsp;&nbsp;
      </li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><% if user_signed_in? %><%= image_tag current_user.picture(:nav), id:'profile-pic' %><% end %>
          <% if current_team.present? %>
            <%= current_team.name %>
          <% end %>
          <span class="caret"></span></a>

        <ul class="dropdown-menu" role="menu">
          <li><%= link_to 'Storyboard', dashboards_index_path %></li>
          <li class="divider"></li>
    <!--       <li><%= link_to 'Switch project', teams_path %></li>
          <li class="divider"></li> -->
          <li><%= link_to 'Account settings', edit_user_registration_path  %></li>
          <li class="divider"></li>
          <li><%= link_to 'Sign me out', destroy_user_session_path, method: 'delete' %></li>
        </ul>
      </li>
    </ul>
  <% else %>
  <%= render partial: "devise/shared/login" %>
<% end %>


