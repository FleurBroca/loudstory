<% answer.comments.each do |comment| %>
  <tr>
    <td><%= comment.user.name %></td>
    <td><%= comment.content %></td>
    <td><%= link_to 'Destroy', answer_comment_path(answer, comment), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>