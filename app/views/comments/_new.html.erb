<%= form_for([answer, answer.comments.new], html: {class: 'form-inline'}) do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.text_area :content, rows: "1", class: "form-control form-comment" %>
  <%= f.submit "Add comment", class: "btn btn-sm btn-blue-small" %>
<% end %>

